import type {IStrandsAgentRuntime,IStrandsAgentRuntimeBootstrap,StrandsAgentRuntimeConfig} from '@tjxjnoobie/strands-bridge'
export class FakeRuntime implements IStrandsAgentRuntime{public invokes:string[]=[];public closeCalls=0;public results:string[]=[];private closed=false;public constructor(result='{}'){this.results=[result]}async invokeAgent(args:any):Promise<any>{this.invokes.push(String(args));return{toString:()=>this.results.shift()??'{}'}}async *streamAgent():AsyncGenerator<any,any,undefined>{return{toString:()=>''}}cancelInvocation():void{}createAgentTool(options?:{name?:string}):any{return{name:options?.name}}isClosed():boolean{return this.closed}async close():Promise<void>{this.closeCalls++;this.closed=true}}
export class FakeBootstrap implements IStrandsAgentRuntimeBootstrap{public configs:StrandsAgentRuntimeConfig[]=[];public runtimes:FakeRuntime[]=[];public constructor(private readonly factory:(config:StrandsAgentRuntimeConfig,index:number)=>FakeRuntime=()=>new FakeRuntime()){}async createAgentRuntime(config:StrandsAgentRuntimeConfig):Promise<IStrandsAgentRuntime>{this.configs.push(config);const runtime=this.factory(config,this.runtimes.length);this.runtimes.push(runtime);return runtime}}

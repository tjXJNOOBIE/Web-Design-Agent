export interface VisualStateData {readonly density:number;readonly spacingScale:number;readonly radius:number;readonly fontScale:number;readonly heroScale:number;readonly contrast:number;readonly depth:number;readonly motion:number}
export const DEFAULT_VISUAL_STATE:VisualStateData={density:.5,spacingScale:1,radius:12,fontScale:1,heroScale:1,contrast:1,depth:.5,motion:.3}

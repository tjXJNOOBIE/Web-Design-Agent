import type {DesignCandidateData} from '../data/DesignCandidateData.js'
import type {DesignGenomeData} from '../data/DesignGenomeData.js'
export interface DesignDistancePairData{readonly left:DesignCandidateData['id'];readonly right:DesignCandidateData['id'];readonly distance:number;readonly passed:boolean}
export interface DesignDistanceResultData{readonly passed:boolean;readonly pairs:readonly DesignDistancePairData[]}
export class DesignDistanceEvaluator{public constructor(private readonly minimumDistance=.45){}public evaluate(candidates:readonly DesignCandidateData[]):DesignDistanceResultData{if(candidates.length!==3)return{passed:false,pairs:[]};const pairs:DesignDistancePairData[]=[];for(const [li,ri] of [[0,1],[0,2],[1,2]] as const){const left=candidates[li],right=candidates[ri];if(left===undefined||right===undefined)continue;const distance=this.distance(left.genome,right.genome);pairs.push({left:left.id,right:right.id,distance,passed:distance>=this.minimumDistance})}return{passed:pairs.length===3&&pairs.every(pair=>pair.passed),pairs}}private distance(left:DesignGenomeData,right:DesignGenomeData):number{const keys:(keyof DesignGenomeData)[]=['composition','navigation','heroStrategy','typography','geometry','surfaceModel','motion','contentRhythm','imageryStrategy'];let score=0;for(const key of keys)score+=left[key]===right[key]?0:1;score+=Math.min(1,Math.abs(left.density-right.density)*2);score+=Math.min(1,Math.abs(left.depth-right.depth)*2);return score/(keys.length+2)}}
